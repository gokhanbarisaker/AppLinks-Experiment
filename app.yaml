application: applinksexperiment
module: default
version: ichi
runtime: python27
api_version: 1
threadsafe: true

handlers:
- url: /.*
  script: handlers.application

libraries:
- name: jinja2
  version: "latest"
